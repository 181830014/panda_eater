vjo.ctype('Usage') //< public

.props({
	//>public void main(String... args) 
	main : function(args){
		new Dependency();
	}
})
.protos({
	
})
.endType();

Warning: "Cannot use type type::Dependency without active needs"

change message to not repeat type 2x and change active needs to active dependency

active dependencies are dependencies which must be loaded before this class can be used