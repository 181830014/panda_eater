Repository provider for ical format (local or over http) would allow to integrate with external calendars, including Google Calendar (which has its own API for submitting tasks). 

BTW, ical format is practically an rss.