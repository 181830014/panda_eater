new local task can be conveniently created using keyboard shortcuts, but there isn't an obvious way to activate newly created task while in the task editor. e.g. Ctrl-F10 popup menu shortcut either doesn't work or don't have the same actions as when popup menu is invoked from the form empty area.