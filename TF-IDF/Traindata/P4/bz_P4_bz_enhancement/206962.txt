In the latest builds I noted there are some actions in task list context menu:

- Import Task
- Export Task
- Import Query
- Export Query

I wonder if it was considered to move them into the standard Import/Export wizards, so they are accessible not only in task list.