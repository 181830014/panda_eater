+++ This bug was initially created as a clone of Bug #192888 +++

In order to make the multiple category suggestion work (esp. category 2 where failures are not contributing to the build tally), we also need to do the following (is some form of this feasible now?):

-every WTP unit test needs to have an associated owner (identified via CVS and/or bugzilla ID)
-whenever that test fails, then "owner" is sent email