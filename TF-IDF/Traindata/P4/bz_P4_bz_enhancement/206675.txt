If you open a bug that is not in your task list, select "Add to task list" using the context menu, the task is reopened using the "full" task editor, but the original remains, ending up with 2 editors for the same task.

The original editor should be closed in this case.