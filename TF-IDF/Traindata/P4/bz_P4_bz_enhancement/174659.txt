From bug 107259 comment#3:
As part of the tooltip, we show the 3 or so highest-interest landmarks for the task context.  This will require a persitent map of task->landmarks, which will be useful for other purposes as well (e.g. context search, bug 137486).