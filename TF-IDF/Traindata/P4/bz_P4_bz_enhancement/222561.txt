If I use the Eclipse refactoring to move a class to a newly created package, the specific task context of the moved class
(methods, nested classes, fields etc.) seems to be lost.

I would expect the context to be also moved to the new package.