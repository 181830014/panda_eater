When I compare a patch set with base on a gerrit review I can change the comparison method (eg. binary, image) however when comparing two patch sets this doesn't work. This was a problem as it used binary comparison for an image which isn't helpful at all.