3.3

If you add a fast view via a perspective factory and the corresponding view is currently blocked via activity disablement no action is taken.  If the activity is later enabled the view will not appear as fast.  Additionally, the closable/movable properties will never be set on the view.