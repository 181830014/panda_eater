Created attachment 224396 [details]
example project

Given the attached example project, modify Mall.stores and add @OneToMany annotation. You get an error that 'Target entity "model.AnimalStore" is not an Entity'.

EclipseLink handles this situation fine, I do not know if this is allowed by the JPA spec or not. See bug 395974, if we fix that bug, we will get this error even for a default 1-many