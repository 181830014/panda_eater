If a connector returns partial task data a task may get updated twice:

1. When the result is returned by performQuery().
2. When a stale task is synchronized

The framework should only update incoming state once and indicate stale tasks while a synchronization is in progress.