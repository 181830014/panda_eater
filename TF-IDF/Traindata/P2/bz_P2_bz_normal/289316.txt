Splitting those two main parts of Mozilla IDE feature will make it possible to install one or other part, whatever user prefers.
They are already quite independent from each other in terms of code. Only common part seems to be SourceLocator.