Two sides:
1. Service which enlists all the *.entity files (produced by the entity service generation) in a way like the Scripting Services, Web, Wiki, etc.
2. UI in Registry that shows the entities available an has a short links to the service's metadata and the service itself.