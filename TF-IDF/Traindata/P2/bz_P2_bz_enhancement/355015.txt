Functionality for parsing text such as commit messages for bug ids should be provided by Tasks and not require a dependency on Context. This affect Builds for instance which has a notion of change sets and provides an action to open corresponding tasks.