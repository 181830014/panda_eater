A connector that I am writing performs queries on the child task level and then gets all parents for the tasks to create a heirarchy.  It would be nice if the framework supported this similar to how is supports the subtasks with dependency relationships.