The current impl has different payload renderer for TEXT, HTML, XML, CSS, JavaScript, and Images. These are statically mapped to a certain mime-types. We need to expose this as some sort of user prefs so that other mime-types can be added and bound to a certain payload renderer.