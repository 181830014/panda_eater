The setting for filtering of subtasks that don't match queries is difficult to discover. Considering the value of the feature it could make sense to enable it by default. To ensure a smooth migration for users with existing Task Lists we could display a message that links to a dialog that explains how to disable the filter to show tasks any tasks that were hidden.