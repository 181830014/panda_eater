The deployment console gives errors on application deployment. In the current implementation, the deployment console is more like machine readable and not friendly to human. The application is not visible here and the event sequence is listed in XML format.

Instead if the application is visible in the deployment perspective, and deployment console gives more human readable like giving errors and warnings, this will make the IDE more user friendly