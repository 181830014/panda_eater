Created attachment 279270 [details]
Test Project to recreate

When running a Testsuite  failure test conditions are executed properly.

When the tests are executed from the UI, the tests defined in Testclasses are not executed and failing tests cases are not found.