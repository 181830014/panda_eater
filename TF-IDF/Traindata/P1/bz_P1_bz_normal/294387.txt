Created attachment 151496 [details]
project/package

The attached project/package contains a config script that references the Mod
module in the package via xdc.useModule('Mod'), but xgconf does not display
this module in the outline view.

Open the config file and notice that Mod is not displayed in the outline view;
it should be.