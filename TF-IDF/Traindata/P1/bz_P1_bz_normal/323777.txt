After (an incomplete) review of setters it appears that it will not be possible to add setters to any delegate of a proxy-delegate pair.

The onSet fails if another setter already exits and setters are implicitly added on delegates.