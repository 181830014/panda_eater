- Resize shapes should invoke auto-size
- Smoother transition to autoscrolling when moving shapes
- Have editboxes invoke auto-scrolling when text grows.
- Connections - creating and editing should invoke auto-scroll
- Drag and drop should invoke auto-scroll.