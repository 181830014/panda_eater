When creating a standard project (GNAT project file created and maintained by the user rather than Hibachi), the user must select the location of the project file which will manage the build.  If this project file already exists, it is erased.