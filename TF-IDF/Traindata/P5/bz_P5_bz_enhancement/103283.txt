The ResourceMarkerListener is currently disabled because it had a bad 
interaction with the Java markers.  This one is going to be tricky, since Java 
compilation units can be resources, so we should work on it together.