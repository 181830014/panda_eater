When we create an empty table with default facets, the dialog "Load an EMF Facet" doesn't allow to display the applied facets.
We need to add an eobject in the table to display the applied facets.

I think it should be interesting to display the applied facets even if the table is empty.