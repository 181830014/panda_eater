Currently the Task activity view has performance issues and can slow systems down when open.  This is due to inefficiency in TaskActivityViewContentProvider.getChildren(). Will either require caching of data or addition of second data structure to TaskListManager that maps Calendar ranges to Tasks.