There is currently no API for registering a listener on the Java project's options.

The UI needs notification about property changes. For example when the Task Tags
change, the syntax highlighting needs to be updated.