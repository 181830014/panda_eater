Build ID: I20070809-1105

Steps To Reproduce:
1. Paste the code below
2. The null check is useless, but no warning is reported



More information:
public class WarningNullCheck {

	public int foo(Object o) {
		if (o != null && o instanceof String)
			return ((String)o).length();
		return 0;
	}
}