Here is an entry for a planned new MoDisco infrastructure layer component

MoDisco should propose a useful mechanism to accelerate M2M writing on Java models : enabling to store a java code portion on any Java model node which would be written back unchanged during the .java generation.