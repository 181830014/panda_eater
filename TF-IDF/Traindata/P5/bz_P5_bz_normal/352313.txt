When adding a query in a query set opened in the model browser using the create query wizard, the queryset model in the model browser disappears (the model browser becomes empty).
This is unfortunate, because QuerySets open by default in the MoDisco model browser (see bug 340282).