Currently the project's files are enumerated, and the size of each file is retrieved for each pass, taking a few unnecessary seconds. This is not needed, since the project's files should not change during the benchmark.