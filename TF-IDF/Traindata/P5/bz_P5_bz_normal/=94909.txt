I20050509-2000 (M7 test pass)

- code assist here:

import static java.lang.Math.a|

gives me four proposals for the different Math.abs({long,double,float,int})
methods. I would expect to only get one proposal as the proposals is for the
method name, not for one of the four in particular.