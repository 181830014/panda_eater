(From Assaf Urieli's comment on bug 64567) Copying source files from one project
(default cp1252) to another project (default utf-8). Ideally, the encoding of
the file should be changed from cp1252 to utf-8, with appropriate conversions.
Instead, the text becomes scrambled.