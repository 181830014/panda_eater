The default tables built use all uppercase for every single mapping, except the m-m uses mixed case for the join table foreign keys.

They should be uppercased.


[EL Fine]: Connection(29422309)--CREATE TABLE POLICYHOLDER_CLAIM (PolicyHolder_ID NUMBER(19) NOT NULL, claims_ID NUMBER(19) NOT NULL, PRIMARY KEY (PolicyHolder_ID, claims_ID))