To support older TopLink deployment XML we will need to implement a conversion tool that will take an EclipseLink/TopLink Project and convert it into an XMLEntityMappings object.

This conversion tool will in turn be used to fulfill the requirement that the Mapping Workbench write out only the new metadata schema format.