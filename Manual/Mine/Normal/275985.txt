The object xdc.cfg.Program.cpu.memoryMap is the program's memory map.  But since cpu can not be displayed the memory map can't be displayed.

The xdc.cfg.Program.cpu object (and all of its instance configs) should be displayable from xgconf.