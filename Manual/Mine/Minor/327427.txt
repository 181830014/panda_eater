Hi,

When using a uiCustomization on a meta-model to hide some meta-class, I noticed that if I hide the root meta-class of my model, no instance is selected at the opening in the MoDisco Model Browser

It could be useful to indicate a meta-class to be selected thanks to an extension point maybe.

Regards

Nicolas Guyomar