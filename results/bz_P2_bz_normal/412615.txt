The Criteria API builds a partial object query when a multiselect is used.  It should not do this, (partial objects are deprecated, and require don't maintain cache, so will not work).

multiselect should return Object[] (even if typed to entity, should get cast error)