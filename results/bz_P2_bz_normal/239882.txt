The package renamer attempts to migrate your xml files to the new format in EclipseLink.

However on Oracle TopLink 10.1.3X xml files, it may get the namespace wrong.

As a work around, use the TopLink 10.1.3 xml files, and manually change the DB platform class name.