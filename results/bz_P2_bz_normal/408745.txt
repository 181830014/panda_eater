Currently, we keep these files around. We really shouldn't, especially as it makes it impossible for users to restore from a situtation where there is bad data (but not bad enough to cause the model to fail to load) in the model itself.