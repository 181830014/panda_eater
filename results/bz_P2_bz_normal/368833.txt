When executing stored procedures using named query, the schema owner name using the SessionCustomizer override session.getLogin().setTableQualifier(schema does not get honoured and as a result, we get stored procedure not found errors.

Support needs to be added defaulting to login's qualifier.