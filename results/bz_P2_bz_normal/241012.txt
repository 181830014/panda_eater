When the state and updated remotely on a task resulting in a change in valid operations, if the user had previously selected an action that is no longer valid, this can result in errors (bug#241008).  We should consider updating the actions at the model level or within the TaskEditorActionPart.