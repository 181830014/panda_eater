Currently our JUnit tests only ensure the correct files, stubs, and dependencies are established in projects built using the toolkit.  We should also create a test that will run the MDR built by the toolkit, and attempt to use the query service and registration service interfaces against it.