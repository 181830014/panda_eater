The AbstractRepositoryConnector.getTaskRelations() method isn't called if the connector returns partial task data.  This means that connectors like the web connector would be unable to show a subtask hierarchy.