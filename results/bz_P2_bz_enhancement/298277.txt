Build Identifier: 

StaticWeaver should accept a Properties object which behaves just like its counterpart in createEMF() so that properties specified in persistence.xml can be overridden during static weaving process. See attached patch.

Reproducible: Always