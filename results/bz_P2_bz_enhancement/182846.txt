Currently, the weave info shows the enclosing class name, the line number, and the aspect used for a particular join point. 

It would be useful to know the enclosing method name, especially for applications using AspectJ to do things that require a comprehensive view of the weaved application before it is run, like coverage tracking.