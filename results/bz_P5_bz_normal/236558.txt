Created attachment 104419 [details]
Report Design

In the attached report, the list has a binding called COUNTRY. The Display property is set to inline. This works in IE. From the viewer export the report to PDF (attached pdf output), the inline property is ignored.

I am using the RC build of 2.3. This is also a problem in 2.2.2.