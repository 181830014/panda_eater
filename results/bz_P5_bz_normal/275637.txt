1. NAMELIST variables should not be renameable

2. Rename allows a local variable to shadow the enclosing program's name (e.g.), but it shouldn't (i.e., scoping is implemented incorrectly in some cases like this)