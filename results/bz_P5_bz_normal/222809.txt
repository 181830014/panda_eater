"serialized", "none" or "class-instance" are reserved converter names that point to specific EclipseLink-implemented converters and handled specially by our metadata processing.  

As a result, if a user specifies a custom @Converter with one of those names, they will get the predefined converter

We should provide a warning to users when this occurs