Reporte through Spring Roo.  For an annotation value that is a class literal where a declare parents ensures the literal actually obeys the bounds of the annotation value, there can be a problem.

This doesn't happen every time - implying it is related to incremental compilation.