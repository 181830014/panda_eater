When I select a particular date e.g. Two Weeks in a scheduled presentation mode a then choose Export option from the context menu the resulting xml.zip file contains no tasks. 
I have to manually select all the tickets under scheduled date.