If you have a relationship to a table per class superclass, the join will only be to th superclass, and not include any subclasses.

Both joining and fetch joining.
Not sure if batch reading works either, should be tested.