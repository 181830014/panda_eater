Currently, if there is more than one "source transition" that lead to the
transition with the guard in question, rtdata is declared as "void *". It
should try to see if there is a common type amongst the triggers of all
potential source transitions and use that instead.