open tutorial config script mon_ED.cfg in local.apps.monitor.  Look at the values of LoggerCallback's config parameters.  The displayed value looks like the name of the config rather than it's value.

select one of the configs to change it and the selections are valid but don't match the value displayed.

The display value of a config should always match one of the available selections.