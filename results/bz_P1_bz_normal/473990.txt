The template service tests were never included in the build. Due to the databinding service refactoring the tests broke. I fixed the tests, but set two of them to @Ignore, because mocking does not work as used any more.  We should review our test strategy here.