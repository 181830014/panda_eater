Today we have found out that some Tables are not created during the first connection to a mysql database if the default encoding is UTF-8.
This is happening for the Testresult and Testresult-summaries since an older Version.