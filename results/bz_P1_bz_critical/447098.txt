We need to update the Java client library for the socket.io websocket connections to a different version or a completely different implementation to improve reliability and to solve threading issues.

We think the socket.io client lib is one of the reasons why the current flux deployment on flux.cfapps.io is not as reliable as it should be.