Current formatter does not allow to specify some advanced options. Such as:

-- Skip whitespace on repeated brackets, eg. ( ( ( 2 + 2 ) / 3) * 4) -> ((( 2 +
2) / 3) * 4)

-- Allow to diferentiate spacing between math and conditional operations, eg. 2
+ 2, but a==b

-- Optimize line wrapping to the corners of statements. E.g. wrap like this:
-----
  someMethod( aaa, bbb, 
    ccc.getValue())

  and not

  someMethod( aaa, bbb, ccc
    .getValue())
-----
  aa==bb ?
    cc : bb

  and not

  aa==bb ? cc :
    bb
------

  I can probably expand this list with few more options