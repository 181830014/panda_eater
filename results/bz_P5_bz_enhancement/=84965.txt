For files that are in a "deep" package, the path is cut off the right side of
the Replace error dialog. This is the dialog that comes up when you do a Search
and click "Replace", then click Replace All. See screenshot.