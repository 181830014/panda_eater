When using *.suite files and many different variables (tab:"Variables") it
would be helpful to be able to jump between lines in each column by using "TAB" or "RETURN". 

Note: Investigate possibility to navigate the SWT table cells with the arrow keys.