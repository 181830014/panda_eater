In a luadocumentation bloc, if you add a new line this will automatically add the "--".

If you want to write a new line of code (not a comment) you should delete this "--" manualy

We should probably find a better solution.