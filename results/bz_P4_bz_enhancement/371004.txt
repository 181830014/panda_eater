Currently, each error might trigger a n error popup dialog.  If therte are multiple elements selected, this can results in multiple dialogs.  Instead error messages should be buffered and presented in a single dialog at the end of the execution