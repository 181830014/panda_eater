BPEL names have some constraints on them which are inconvenient for users.  For example, they can only use certain characters, and they might need to be unique within certain contexts.

We should provide a generic mechanism for putting user-friendly labels on activities and other canvas objects.