When importing new commit review items, for some files, especially new and deleted files, the platformURI is not always populated, so the project path is missng from the FileContext properties for these files.  We need to investigate to see if there is a way to get the path at creation.